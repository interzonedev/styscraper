(function() {
    "use strict";

    /*
     * Define (or get if it was already defined) the module in which to register application wide directives.  This will
     * make all registered directives available to an application that includes this as a dependency.
     */
    izng.module("styscraper.Directives", []);

    // Common directives code goes here.

}());
